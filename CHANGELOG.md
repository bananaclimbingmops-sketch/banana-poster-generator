# 香蕉换线海报生成器 — 版本日志

> 版本号遵循语义化版本规范（Semantic Versioning）：`主版本.次版本.补丁版本`
> 次版本号升级 = 新增功能；补丁版本号升级 = Bug 修复或小改动

---

## v1.8.0（当前版本）
**提交：** `87df048` | **新增 9:16 手机尺寸**

新增适合手机屏幕分享的竖版尺寸选项。海报尺寸选择器新增「9:16 手机」，导出 PNG 分辨率为 1080×1920px。尺寸按钮从 3 列改为 2×2 网格排列，操作指南同步更新尺寸说明。

---

## v1.7.0
**提交：** `1ab1aff` | **胶囊布局扩展 + 移动端响应式适配**

贴纸胶囊宽度扩展至 980px，定线员姓名文字自动缩字适配圆形边界（无空格时）或自动换行（有空格时）。修复 iOS Safari 无法通过 `<a download>` 下载 data URL 的问题，改用 Blob URL 方式。`pixelRatio` 限制上限为 6，防止手机端内存溢出。手机端 Header 改为两行布局（标题在上，按钮在下），桌面端保持左右排列。

---

## v1.6.0
**提交：** `c76200b` | **操作指南 + 4种定线员身份 + Logo 切换**

操作指南整合到导航栏侧边抽屉（GuideDrawer），无需跳转外部页面。定线员身份扩展为4种（特邀国际/国内定线员、香蕉定线员、香蕉教练员），特邀国际定线员采用黑色卡片样式。新增 Logo 切换功能（香蕉攀岩 / BANANA+ BOULDERING）。多次换线模式支持批量导入，定线员按身份优先级自动排序。修复国旗图标圆形边缘黑边问题（4x 超采样抗锯齿）。

---

## v1.5.0
**提交：** `0fafbd1` | **定线员贴纸生成器上线**

新增独立的定线员贴纸生成器页面。支持生成 945×945px 圆形贴纸（8×8cm @300dpi），AI 自动抠图去背景，人脸自动定位构图，手动拖拽微调（位置 + 缩放滑块），国旗图标叠加显示。

---

## v1.4.0
**提交：** `2d60e0a` | **多次换线模式 + 一键清空**

新增多次换线模式，时间表以结构化日期卡片网格呈现，适合跨多天的换线计划。定线员列表新增一键清空按钮（含二次确认弹窗）。批量导入新增 ZIP 压缩包上传支持。标签字体改用 `cqh` 单位随容器高度自动缩放。

---

## v1.3.0
**提交：** `9c4367a` | **批量导入 + 历史记录 + Favicon**

新增批量导入定线员功能，支持文件夹上传和 `info.json` 解析。新增生成记录历史库，自动保存最近 20 条记录到浏览器 localStorage，含缩略图和完整配置，可一键恢复。新增网站 Favicon，支持深色/浅色模式自动切换。

---

## v1.2.0
**提交：** `e4c8b68` | **编辑弹窗 + 图片裁切 + 新增尺寸**

已添加的定线员支持通过编辑弹窗修改所有字段（照片、姓名、简介、身份、国籍）。编辑弹窗内置图片裁切功能（react-image-crop）。新增 59×79cm 导出尺寸。

---

## v1.1.0
**提交：** `c465c92` | **前端 WASM 抠图 + 拖拽排序**

AI 抠图从 Railway 后端迁移至浏览器端 WebAssembly（@imgly/background-removal），彻底解决服务器 CPU 超时问题，抠图速度大幅提升。修复定线员列表拖拽排序失效问题。修复 html-to-image 导出失败（图片改用 base64 存储）。修复下载海报字体丢失问题。

---

## v1.0.0
**提交：** `9afe21d` | **初始版本上线**

换线海报生成器首次上线。支持海报基础信息填写（标题、副标题、换线时间表、闭馆/不闭馆、换线区域），定线员添加（照片上传、AI 抠图、姓名、简介、身份、国籍），实时预览，PNG/PDF 导出（60×90cm）。Railway 后端 + Vercel 前端双平台部署。

---

## 版本回退方法

如需回退到某个历史版本，在 GitHub 仓库中找到对应 Tag，或使用以下命令：

```bash
# 查看所有版本标签
git tag -l "v*"

# 查看某个版本的详细信息
git show v1.6.0

# 回退到指定版本（仅本地）
git checkout v1.6.0

# 强制将 master 分支回退到指定版本（谨慎操作）
git reset --hard v1.6.0
git push origin master --force
```
